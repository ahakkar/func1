{
    "version": "2.0.0",
    "tasks": [
      {
        "label": "Compile and Run Haskell",
        "type": "shell",
        "command": "ghc ${file} -o ${fileBasenameNoExtension} && ./${fileBasenameNoExtension}",
        "options": {
            "env": {
              "PATH": "/home/antti/.ghcup/bin:/usr/local/bin:/usr/bin:/bin:/usr/local/games:/usr/games:/home/antti/.cabal/bin"
            }
        },
        "group": {
          "kind": "build",
          "isDefault": true
        }
      }
    ]
  }